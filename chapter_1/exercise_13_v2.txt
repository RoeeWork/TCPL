#include <stdio.h>
#define MAX_WORDS 100

main()
{
	int c, i, max_arr[MAX_WORDS];
	while((c = getchar()) != EOF)
	{
		if(c != ' ' && c != '\t' && c != '\n')
		{
			max_arr[i] = 0;
		}
		else
		{
			++i;
		}
	}
}
